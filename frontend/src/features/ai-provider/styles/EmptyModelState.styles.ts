import type { SxProps, Theme } from "@mui/material";

export const emptyModelStateStyles: Record<string, SxProps<Theme>> = {
    container: {
        p: 6,
        textAlign: "center",
        borderStyle: "dashed",
        opacity: 0.5,
    }
}