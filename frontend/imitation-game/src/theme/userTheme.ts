import { createTheme } from "@mui/material";

export const userTheme = createTheme({
    palette: {
        mode: "dark",

        primary: {
            main: "#2196f3"
        },

        secondary: {
            main: "#00bcd4"
        }
    }
});