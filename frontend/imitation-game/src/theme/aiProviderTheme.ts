import { createTheme } from "@mui/material";

export const aiProviderTheme = createTheme({
    palette: {
        mode: "dark",

        primary: {
            main: "#f44336"
        },

        secondary: {
            main: "#ff9800"
        }
    }
});