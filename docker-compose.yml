services:
  db:
    image: postgres:15.3
    volumes:
      - ./volumes/postgres/data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: luiz
      POSTGRES_USER: luiz
      POSTGRES_DB: imitation-game-sql
    ports:
      - "5432:5432"